# 项目优化清单

> 这个清单用于持续迭代。每一项都应尽量先补回归测试，再做小步改动。

## 已完成

- [x] 前端结构整理：脚本集中到 `js/`，样式集中到 `css/`，主题样式集中到 `css/themes/`。
- [x] 设置相关样式从全局 `styles.css` 拆到 `css/settings.css`。
- [x] AI 助手相关样式从全局 `styles.css` 拆到 `css/ai.css`。
- [x] 日历相关样式从全局 `styles.css` 拆到 `css/calendar.css`。
- [x] 音乐播放器相关样式从全局 `styles.css` 拆到 `css/music.css`。
- [x] 天气相关样式从全局 `styles.css` 拆到 `css/weather.css`。
- [x] 首页导航卡片和分组相关样式从全局 `styles.css` 拆到 `css/cards.css`。
- [x] 静态资源清单抽到 `js/static-assets.js`，让 Service Worker 与测试共用同一份缓存资源列表。
- [x] 设置主题切换的纯逻辑抽到 `js/settings-ui.js`。
- [x] Docker 构建上下文瘦身：新增 `.dockerignore`，排除 `.git/`、`data/`、`node_modules/`、测试目录和日志。
- [x] JS 语法检查脚本化：`npm run check` 改为自动发现 `js/`、`server/`、`scripts/` 和 `sw.js` 中的 JS 文件。
- [x] 同步设置纯逻辑抽取：新增 `js/sync-ui.js`，让 WebDAV/Gist/备份选项的表单归一化和状态文案可单测。
- [x] 背景设置纯逻辑抽取：新增 `js/background-ui.js`，让背景 URL 列表、视频判断、文件大小、缓存参数和上传类型判断可单测。
- [x] 导航主状态和设置读写收敛到 `js/storage-adapter.js`，让 `app.js` 不再直接调用业务 `localStorage` API。
- [x] 博客模块读写收敛到 `js/storage-adapter.js`，示例内容同步改为服务端持久化表述。
- [x] 天气模块城市、主城市和缓存数据读写收敛到 `js/storage-adapter.js`。
- [x] 日历模块任务、重复规则和提醒设置读写收敛到 `js/storage-adapter.js`。
- [x] AI、音乐、同步模块读写收敛到 `js/storage-adapter.js`，业务数据入口统一。
- [x] 渲染安全工具抽到 `js/render-utils.js`，统一 HTML 转义、URL 属性和 CSS 颜色白名单处理。
- [x] 添加网址/分组表单逻辑抽到 `js/link-ui.js`，降低 `app.js` 中表单归一化与 option 拼接耦合。
- [x] AI 面板选项、附件、空状态、操作卡渲染抽到 `js/ai-ui.js`。
- [x] 日历月视图、任务项、即将到来项、统计图渲染抽到 `js/calendar-ui.js`。
- [x] 服务端上传入口增加按类别的 MIME / 扩展名白名单、大小限制和 JSON 错误中间件。
- [x] 新增 `npm run smoke:browser`，启动临时本地静态服务检查首页和关键 UI 资源可达。
- [x] AI 对话扩展：新增图片图库，支持从历史回复收集图片、预览、保存、复制提示词与再生成。
- [x] 数据与备份扩展：服务端 SQLite 新增快照表与 `/api/snapshots`，支持创建、校验和恢复快照。
- [x] 导航体验扩展：新增链接健康检查接口与首页入口，卡片显示健康状态点。
- [x] 搜索扩展：本地过滤改为智能匹配名称、URL、描述、标签和分组。
- [x] 日历扩展：新增周视图，并保留任务完成、跳过、编辑等操作。
- [x] 主题扩展：设置中新增自定义主题草稿，可保存主题名、主色与粒子特效。
- [x] 插件化基础：新增声明式功能模块注册表，设置中展示模块状态。
- [x] PWA 扩展：Service Worker 为离线启动增加文档回退。
- [x] 继续拆分 `js/app.js`：新增模块边界注册表与命令、AI、媒体、版本、懒初始化、无障碍等纯逻辑模块。
- [x] 媒体清理增强：服务端提供孤儿媒体扫描与安全删除接口，设置存储页可直接清理未引用文件。
- [x] 浏览器端冒烟测试继续扩展：覆盖服务端数据读写、关键按钮、命令面板与新增静态模块。
- [x] 设置弹窗可访问性：新增焦点陷阱、按钮标签审计工具，并为所有 dialog 统一增强键盘行为。
- [x] 性能优化：使用懒初始化注册器收敛日历、天气、同步、建议、最近使用等非首屏初始化边界。
- [x] 文档精简：README 继续保留入口信息，架构、存储、部署、功能与测试拆入 `docs/`。
- [x] AI 操作预览与回滚：AI 指令卡展示变更预览，应用前创建快照，支持撤销本次操作。
- [x] 命令面板：新增 Ctrl/Cmd+Shift+P 命令面板，可快速打开设置、日历、AI、音乐、分组与维护动作。
- [x] 服务端数据版本ing：快照支持对比当前数据与按分类恢复，降低整包回滚风险。
- [x] 插件生命周期接口：功能注册表补充 register/start/stop/dispose 生命周期，后续插件可按状态接入。

## P0：优先优化

- [ ] 继续物理拆分 `js/app.js`：天气 UI、博客 UI 和设置事件绑定可在已有边界基础上迁出。

## P1：质量与安全

- [ ] 动态 HTML 渲染继续收敛：天气、博客、最近使用、星标列表后续可继续迁移到 `render-utils.js` 或 DOM 构造函数。
- [ ] 媒体清理后台策略：在清理前自动创建快照，并支持定期报告而非自动删除。
- [ ] 浏览器端冒烟测试继续扩展：引入真正的 DOM 自动化，覆盖设置弹窗、主题切换与日历打开后的交互。

## P2：体验与长期维护

- [ ] 设置弹窗可访问性：继续补足关键图标按钮 `aria-label`，并做屏幕阅读器抽样检查。
- [ ] 性能优化：继续把博客和 AI 的重型事件绑定改成首次打开时初始化。
- [ ] 构建检查扩展：增加静态资源可达性检查，确保 Service Worker 缓存路径与实际文件保持一致。
- [ ] 文档自动校验：增加链接检查，确保 README 与 `docs/` 中的命令、路径持续可用。
